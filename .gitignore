# OpenTofu / Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Environment files (generated by OpenTofu)
.env
.env.local
.env.*.local

# Docker
docker-compose.override.yml

# Database backups
# Ignore all backup files
backups/*.sql
backups/*.dump
backups/*.gz

# EXCEPT: Track the production backup (distributed via Git)
!backups/production_backup_data_only.sql

# Keep backups directory structure
!backups/.gitkeep
!backups/README.md

# Note: If backup grows >50MB, consider Git LFS:
# git lfs track "backups/production_backup_data_only.sql"

# Data files (CSV, etc)
data/*.csv
data/*.tsv
data/*.xlsx
data/*.json
!data/README.md

# PostgreSQL data
postgres_data/
pgadmin_data/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS
Thumbs.db
Desktop.ini

# Logs
*.log
logs/

# Temporary files
*.tmp
*.temp
.cache/

# Secrets and keys
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
